package com.opytha.weatherAPI.models.enums;

public enum Units {
    STANDARD,   // STANDARD UNIT SYSTEM
    METRIC,     // METRIC SYSTEM (KG, M, °C)
    IMPERIAL    // IMPERIAL SYSTEM (LB, FT, °F)
}
